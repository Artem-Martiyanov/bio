let switchCount = 0export const onClickHandler = (event) => {  const dataType = event.target.dataset.type    if (dataType === 'morpheus') {    const dialog = document.querySelector('.intro-page__dialog')    dialog.classList.add('intro-page__dialog--down')  }    if (dataType === 'burger' || event.target.closest('[data-type="burger"]') || dataType === 'nav') {    const burger = document.querySelector('.burger')    const nav = document.querySelector('.nav')    burger.classList.toggle('burger--active')    nav.classList.toggle('nav--toggle')  }    if (dataType === 'lamp-switcher') {    const lamp = document.querySelector('.lamp')    const switcher = document.querySelector('[data-type="lamp-switcher"]')    const electrician = document.querySelector('.works-page__electrician')    switchCount++    if (switchCount >= 2) {      electrician.classList.add('works-page__electrician--shown')      if (switchCount > 5) {        switcher.setAttribute('disabled', 'disabled')        lamp.classList.add('lamp--change')        lamp.addEventListener('animationend', (event) => {          if (event.animationName === 'lamp-change') {            lamp.classList.remove('lamp--change')            switcher.removeAttribute('disabled')            electrician.classList.remove('works-page__electrician--shown')          }        })        switchCount = 0      }    }    lamp.classList.toggle('lamp--toggle')  }}