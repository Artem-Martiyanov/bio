import {Particle} from './particle/particle';export class ParticleGenerator {    constructor(particleSize, particleSizeDispersion, innerRadius, outerRadius, particleColor, spawnRate = 100) {    this.prevFramesTime = null    this.spawnRate = spawnRate    this.settings = [      particleSize,      particleSizeDispersion,      innerRadius,      outerRadius,      particleColor    ]    window.requestAnimationFrame(this.#render.bind(this))  }    #render(curFramesTime) {    if (!this.prevFramesTime) {      this.prevFramesTime = curFramesTime    }        if (curFramesTime - this.prevFramesTime >= this.spawnRate) {      new Particle(...this.settings)      this.prevFramesTime = curFramesTime    }    window.requestAnimationFrame(this.#render.bind(this))  }}